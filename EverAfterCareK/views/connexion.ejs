<!DOCTYPE html>
<html lang="fr">

<head>
    <%- include('./partials/head'); %>
</head>

<header>
    <%- include('./partials/header'); %>
</header>

<body>
    
    <div class="containerConnexion1">
        <h1>Connexion</h1>
      <% if (messages) { %>
        <p><%= messages.error %></p> 
        <% } %>
        <form action="/connexion" method="POST">
            <input type="email" name="email" placeholder="Adresse courriel" id="champ1" onkeyup="verifierChamps()"/>
            <input type="password" name="password" placeholder="Mot de passe" id="champ2" onkeyup="verifierChamps()"/>
            <button id="button">Connexion</button>
        </form>

    </div>

    <script>
        function verifierChamps() {
            if (champ1.value.length > 0 && champ2.value.length > 0) {
                document.getElementById('button').disabled = false;
            } else {
                document.getElementById('button').disabled = true;
            }
        }
    </script>

</body>
</html>

<%- include('partials/footer') %>